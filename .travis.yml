language: python

python:
  - 3.8

services:
  - docker

before_script:
  - docker-compose up -d

script:
  - docker-compose exec web coverage run manage.py test apps

after_success:
  - docker-compose exec -e COVERALLS_REPO_TOKEN=$coveralls_repo_token web coveralls
